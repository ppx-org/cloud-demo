
0. mid为固定值

1.scan带参数?sid=1&c=A01

var c = '1|A01';
document.write(window.btoa(c));
try {
	var ss = window.atob("MXxBMDE=");
	var para = ss.split("|");
	var sid = para[0];
	var code = para[1];
	alert("out:" + sid + "|" + code);
}
catch(e) {
	document.write("error.........1");
}

2.不带，sid和mid为默认值，从后端返回，（可直接在页面上写死?）


onLaunch:function() {
	promoCode url scene date
}










































// barcode
code: https://github.com/demi520/wxapp-qrcode



































体验:
搜索维护时提示系统忙
图片服务出问题时，提示





