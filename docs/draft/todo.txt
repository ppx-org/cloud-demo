1.scan带参数?sid=1&mid=1&c=A

var c = '1|1|A01';
document.write(window.btoa(c));
try {
	var ss = window.atob("MXwxfEEwMQ==");
	var para = ss.split("|");
	var sid = para[0];
	var mid = para[1];
	var code = para[2];
	alert("out:" + sid + "|" + mid + "|" + code);
}
catch(e) {
	document.write("error.........1");
}

2.不带，sid和mid为默认值，从后端返回，（可直接在页面上写死?）



































// barcode
code: https://github.com/demi520/wxapp-qrcode



































体验:
搜索维护时提示系统忙
图片服务出问题时，提示





